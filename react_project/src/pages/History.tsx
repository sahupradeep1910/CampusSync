import {useState} from 'react'; import {Search,CalendarDays,RotateCcw,CheckCircle,Clock3,Users,ArrowUpRight} from 'lucide-react'; import {useGlobal} from '../context/GlobalContext'; import {Link} from 'react-router-dom';
export default function History(){const{borrowHistory,returnResource}=useGlobal();const[q,setQ]=useState('');const[tab,setTab]=useState('All');const filtered=borrowHistory.filter(r=>(tab==='All'||(tab==='Active'&&!r.returned)||(tab==='Returned'&&r.returned))&&`${r.studentName} ${r.studentId} ${r.resourceName}`.toLowerCase().includes(q.toLowerCase()));const date=(s:string)=>new Date(s).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'});return <div className="history-page"><section className="page-hero"><div><span className="section-kicker">ACTIVITY CENTER</span><h1>Borrowing history</h1><p>Track active loans, completed returns and student resource activity.</p></div><div className="history-summary"><div><Users size={17}/><b>{borrowHistory.length}</b><span>Total records</span></div><div><Clock3 size={17}/><b>{borrowHistory.filter(x=>!x.returned).length}</b><span>Active loans</span></div></div></section><section className="history-panel"><div className="history-toolbar"><div className="search-box"><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search student, ID or resource..."/></div><div className="filter-pills">{['All','Active','Returned'].map(x=><button key={x} className={tab===x?'selected':''} onClick={()=>setTab(x)}>{x}</button>)}</div></div><div className="table-wrap"><table><thead><tr><th>Student</th><th>Resource</th><th>Borrowed</th><th>Status</th><th>Action</th></tr></thead><tbody>{filtered.map(r=><tr key={r.id}><td><div className="student-cell"><span className="avatar small">{r.studentName.split(' ').map(x=>x[0]).slice(0,2).join('')}</span><span><b>{r.studentName}</b><small>{r.studentId}</small></span></div></td><td><Link className="resource-link" to={`/resource/${r.resourceId}`}>{r.resourceName}<ArrowUpRight size={14}/></Link></td><td><span className="date-cell"><CalendarDays size={15}/>{date(r.borrowDate)}</span></td><td><span className={`status ${r.returned?'returned':'active'}`}>{r.returned?<CheckCircle size={13}/>:<Clock3 size={13}/>} {r.returned?'Returned':'Active'}</span></td><td>{r.returned?<span className="muted">Completed {r.returnDate?date(r.returnDate):''}</span>:<button className="return-btn" onClick={()=>returnResource(r.resourceId)}><RotateCcw size={14}/> Mark returned</button>}</td></tr>)}</tbody></table>{!filtered.length&&<div className="empty-state"><Search size={34}/><h3>No activity found</h3><p>Try changing your search or status filter.</p></div>}</div></section></div>}
