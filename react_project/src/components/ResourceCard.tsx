import {useState} from 'react'; import {Link} from 'react-router-dom'; import {BookOpen,Monitor,MapPin,Star,ArrowUpRight,CheckCircle,Clock,UserRound,X} from 'lucide-react'; import type {Resource} from '../types'; import {useGlobal} from '../context/GlobalContext';
export const ResourceCard=({resource}:{resource:Resource})=>{const{borrowResource}=useGlobal();const[open,setOpen]=useState(false);const[form,setForm]=useState({name:'',id:''});const submit=()=>{if(form.name.trim()&&form.id.trim()){borrowResource(form.name.trim(),form.id.trim(),resource.id);setOpen(false);setForm({name:'',id:''})}};return <>
<div className="resource-card"><div className="card-top"><div className={`resource-icon ${resource.type==='Book'?'book':'equipment'}`}>{resource.type==='Book'?<BookOpen/>:<Monitor/>}</div><span className={`status ${resource.isAvailable?'available':'borrowed'}`}>{resource.isAvailable?<CheckCircle size={13}/>:<Clock size={13}/>} {resource.isAvailable?'Available':'On loan'}</span></div><div className="type-line">{resource.category} <span>•</span> {resource.type}</div><Link to={`/resource/${resource.id}`} className="resource-name">{resource.name}<ArrowUpRight size={17}/></Link><p>{resource.description}</p><div className="meta-row"><span><MapPin size={14}/>{resource.location}</span><span><Star size={14} fill="currentColor"/>{resource.rating}</span></div><div className="card-footer"><span className="usage">{resource.usageCount} checkouts</span><button className="primary-btn small" disabled={!resource.isAvailable} onClick={()=>setOpen(true)}>{resource.isAvailable?'Borrow now':'Unavailable'}</button></div></div>
{open&&<div className="modal-backdrop" onMouseDown={()=>setOpen(false)}><div className="modal" onMouseDown={e=>e.stopPropagation()}><button className="modal-close" onClick={()=>setOpen(false)}><X/></button><div className="modal-icon"><UserRound/></div><h2>Borrow resource</h2><p>Enter student details to create an active loan for <b>{resource.name}</b>.</p><label>Student name<input autoFocus value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="e.g. Pradeep Sahu"/></label><label>Student ID<input value={form.id} onChange={e=>setForm({...form,id:e.target.value})} placeholder="e.g. S001"/></label><button className="primary-btn full" onClick={submit}>Confirm Borrow <ArrowUpRight size={17}/></button></div></div>}
</>}
